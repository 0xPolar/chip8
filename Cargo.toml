[package]
name = "chip8"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10.0"
raylib = "5.5.1"
rodio = "0.19"
eframe = { version = "0.34.1", default-features = false, features = ["glow", "default_fonts"] }
